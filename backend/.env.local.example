# Local development — both URLs point directly to the Docker Postgres container
DATABASE_URL="postgresql://postgres:postgres@localhost:5433/monstergql"
DIRECT_URL="postgresql://postgres:postgres@localhost:5433/monstergql"
